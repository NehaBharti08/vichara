Assess progress and decide what to do next.

Task: {task}

What has happened:
{recent}

{trigger}

Choose one:

- `continue` -- the current approach is working.
- `revise` -- the plan is wrong and a different approach is needed. Say what.
- `give_up` -- no available tool can answer this. Choosing this early is
  correct when it is true; grinding through more steps to reach the same
  conclusion wastes the budget and is scored as a failure.

Be concrete about *why*. "Retrieval returned nothing for three different
phrasings, so the corpus does not cover this" is useful. "Continue trying" is
not.
